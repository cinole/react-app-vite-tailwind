// autoload all style
import './_autoload.scss'
// const autoload = import.meta.glob('./_autoload.scss', { eager: true })
// import font face
import '@/assets/fonts/style.css'

// load all layout styles
// import.meta.glob("../layout/*.scss", { as: "url", eager: true })
// import.meta.glob("../layout/**/*.scss", { as: "url", eager: true })

// load all component styles
// import.meta.glob("../components/**/*.scss", { as: "url", eager: true })
// const modules = import.meta.glob('@/components/**/*.scss')
// Object.keys(modules).map((key) => modules[key]())

// load all module styles
// const modules = import.meta.glob('../view/**/*.scss')
// Object.keys(modules).map(key => modules[key]())
